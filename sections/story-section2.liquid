{% schema %}
{
  "name": "Story Section2",
  "settings": [
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Left Side Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Our Story"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content"
    }
  ],
  "presets": [
    {
      "name": "Story Section2",
      "category": "Our Story"
    }
  ]
}
{% endschema %}

<section class="our-story-banner story-section2">
  <div class="left-image">
    {% if section.settings.banner_image != blank %}
      <img src="{{ section.settings.banner_image | image_url: width: 1200 }}" alt="Banner Image">
    {% endif %}
  </div>
  <div class="page-width">
    <div class="story-banner-wrap">
      <div class="left-content"></div>
      <div class="right-content">
        {% if section.settings.heading != blank %}
          <h2>{{ section.settings.heading }}</h2>
        {% endif %}
  
        {% if section.settings.content != blank %}
          <div class="rich-text">
            {{ section.settings.content }}
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</section>